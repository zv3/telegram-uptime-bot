{
  "name": "telegram-uptime-bot",
  "version": "1.0.0",
  "description": "AWS lambda function to notify via Telegram the configured website is down",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["uptime bot", "aws lambda", "telegram node bot"],
  "author": "dzv3",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.1.1",
    "node-telegram-bot-api": "^0.40.0"
  }
}
